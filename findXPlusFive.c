#include <stdio.h>

int main() {
    int num;
    scanf("%d", &num);
    // Don't change above this line

    int x = 0;
    if(num > 5)
    {
        x += 5;
    }
    
    // Don't change below this line
    printf("x = %d", x);
    return 0;
}
